<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.agzuniverse.agz.opensalve">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".HomeActivity"
            android:theme="@style/HomeScreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".CampMgmtScreen"
            android:label="Camp Management"
            android:parentActivityName=".HomeActivity"
            android:theme="@style/CampMgmtScreenTheme" />
        <activity
            android:name=".ListOfInhabitants"
            android:label="List of Inhabitants"
            android:parentActivityName=".CampMgmtScreen"
            android:theme="@style/ListOfInhabitantsScreenTheme" />
    </application>

</manifest>